//
// Created by Han on 2022/11/16.
//
#include <stdio.h>

int main(void)  {
    int yw,sx,yy,sum,b;
    scanf("%d%d%d",&yw,&sx,&yy);

    sum = yw + sx + yy;
    b = sum/3;
    printf("%d,%d",sum,b);

    scanf("%d%d%d",&yw,&sx,&yy);

    sum = yw + sx + yy;
    b = sum/3;
    printf("%d,%d",sum,b);

    scanf("%d%d%d",&yw,&sx,&yy);

    sum = yw + sx + yy;
    b = sum/3;
    printf("%d,%d",sum,b);
}
